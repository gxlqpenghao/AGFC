# Fixtures

AGFC demos generate public-safe fixtures on demand under the chosen output directory.

Use:

```bash
agfc demo extract
agfc demo mineru
```

The repository avoids checking in private PDFs or large generated artifacts.
